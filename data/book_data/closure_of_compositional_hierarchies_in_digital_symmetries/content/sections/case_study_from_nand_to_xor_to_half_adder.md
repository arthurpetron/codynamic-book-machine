\(\varepsilon(r)\): gate-level; hazards via timing-window violation rate

Deriving \(\neg, \wedge, \vee\) from NAND; depth/size trade-offs

#### XOR: \(a \oplus b \equiv (a \wedge \neg b) \vee (\neg a \wedge b)\) and alts

Half-adder: spec, impl, invariances (swap a,b)

#### Artifact: Verilog + testbench; \(\varepsilon(r)\) under faults
