### Claims-as-builds: theorems paired with executable artifacts/tests

### Acquire–Calibrate–Log: (1) acquire specs/datasets, (2) calibrate \(\varepsilon(r)\), (3) log reproducibility (hashes, seeds, toolchains)

### Reading map: dependency graph across chapters for varied backgrounds
