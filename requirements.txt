# Codynamic Book Machine - pinned Python dependencies
#
# Rebuild the local environment with:
#   python3 -m venv .venv
#   ./.venv/bin/python -m pip install -r requirements.txt

# Core LLM providers
openai==2.38.0
anthropic==0.104.1

# Data handling
pyyaml==6.0.3
python-dateutil==2.9.0.post0
jsonschema==4.26.0  # For schema validation

# Testing
pytest==9.0.3
pytest-cov==7.1.0
pytest-mock==3.15.1

# Development tools
black==26.5.1
pylint==4.0.5
mypy==2.1.0

# LaTeX compilation support (system dependencies)
# Note: Requires pdflatex/latexmk installed separately
# MacTeX: https://www.tug.org/mactex/
# MiKTeX (Windows): https://miktex.org/
# TeX Live (Linux): sudo apt-get install texlive-full

# Optional: Image processing for PDF rendering
# Pillow>=10.0.0
# pdf2image>=1.16.0
